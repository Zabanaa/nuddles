module.exports = {
    clientId: 'my_client_id' || process.env['CLIENT_ID'],
    clientSecret: 'my_client_secret' || process.env['CLIENT_SECRET'],
    accessToken: "my_access_token" || process.env['ACCESS_TOKEN'],
    redirectUri: "http://localhost"
}

